#pragma once

#define BASE_ANONYMOUS_NAME_CONCAT(a, b) BASE_ANONYMOUS_NAME_CONCAT_INNER(a, b)
#define BASE_ANONYMOUS_NAME_CONCAT_INNER(a, b) a##b

#define ANONYMOUS_NAME(prefix) BASE_ANONYMOUS_NAME_CONCAT(prefix, __LINE__)